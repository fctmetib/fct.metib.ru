import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'contacts',
  template: `
  <div class="contacts">
    <h2>Контакты</h2>
  </div>`
})

export class ContactsComponent implements OnInit {
  constructor() { }

  ngOnInit() { }
}
